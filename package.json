{
  "name": "portfolio-site",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i src/input.css -o dist/output.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "tailwindcss": "^4.1.0"
  }
}
